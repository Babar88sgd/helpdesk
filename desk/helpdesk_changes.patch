diff --git a/desk/package.json b/desk/package.json
index 0e11e95..f667b68 100644
--- a/desk/package.json
+++ b/desk/package.json
@@ -27,7 +27,7 @@
     "dayjs": "^1.11.7",
     "echarts": "^6.1.0",
     "grid-layout-plus": "^1.1.0",
-    "frappe-ui": "1.0.0-beta.63",
+    "frappe-ui": "1.0.0-beta.75",
     "mime": "^3.0.0",
     "pinia": "^2.0.33",
     "sanitize-html": "^2.10.0",
diff --git a/desk/src/components/CommunicationArea.vue b/desk/src/components/CommunicationArea.vue
index f24edd5..f815352 100644
--- a/desk/src/components/CommunicationArea.vue
+++ b/desk/src/components/CommunicationArea.vue
@@ -7,7 +7,7 @@
         <Button
           ref="sendEmailRef"
           variant="ghost"
-          label="Reply"
+          label="Comment"
           :class="[
             showEmailBox ? '!bg-surface-gray-4 hover:!bg-surface-gray-3' : '',
           ]"
@@ -19,7 +19,7 @@
         </Button>
         <Button
           variant="ghost"
-          label="Comment"
+          label="Reply"
           :class="[
             showCommentBox ? '!bg-surface-gray-4 hover:!bg-surface-gray-3' : '',
           ]"
@@ -84,10 +84,10 @@
             ref="commentTextEditorRef"
             :label="
               isMobileView
-                ? 'Comment'
+                ? 'Reply'
                 : isMac
-                ? 'Comment (⌘ + ⏎)'
-                : 'Comment (Ctrl + ⏎)'
+                ? 'Reply (⌘ + ⏎)'
+                : 'Reply (Ctrl + ⏎)'
             "
             :ticketId="ticketId"
             :editable="showCommentBox"
diff --git a/desk/src/components/ListRows.vue b/desk/src/components/ListRows.vue
index e0f31d4..d8231e7 100644
--- a/desk/src/components/ListRows.vue
+++ b/desk/src/components/ListRows.vue
@@ -36,7 +36,10 @@
           :key="row.name"
           v-slot="{ idx, column, item }"
           :row="row"
-          class="truncate text-base row"
+          :class="[
+            'truncate text-base row',
+            highlightUnread && isUnreadRow(row) && 'hd-unread-row',
+          ]"
         >
           <slot v-bind="{ idx, column, item, row }" />
         </ListRow>
@@ -49,7 +52,10 @@
       :key="row.name"
       v-slot="{ idx, column, item }"
       :row="row"
-      class="truncate text-base"
+      :class="[
+        'truncate text-base',
+        highlightUnread && isUnreadRow(row) && 'hd-unread-row',
+      ]"
     >
       <slot v-bind="{ idx, column, item, row }" />
     </ListRow>
@@ -66,7 +72,11 @@ import {
 } from "frappe-ui/experimental";
 import { computed, ref, watch } from "vue";
 
+import { useAuthStore } from "@/stores/auth";
 import IconMoreHorizontal from "~icons/lucide/more-horizontal";
+
+const { userId } = useAuthStore();
+
 const props = defineProps({
   rows: {
     type: Array,
@@ -76,8 +86,21 @@ const props = defineProps({
     type: Array,
     default: () => [],
   },
+  // When true, a row that the current user has not yet seen (per the
+  // framework's `_seen` field) is rendered with a solid black background
+  // and white text, instead of the default light bold treatment.
+  highlightUnread: {
+    type: Boolean,
+    default: false,
+  },
 });
 
+function isUnreadRow(row) {
+  if (!row) return false;
+  const seenBy = row._seen ? JSON.parse(row._seen) : [];
+  return !seenBy.includes(userId || "");
+}
+
 const groupedRows = ref(props.rows);
 
 const actions = (group) => {
@@ -105,4 +128,13 @@ let showGroupedRows = computed(() => {
 });
 </script>
 
-<style></style>
+<style>
+/* Unread ticket row: solid black background, white text everywhere inside
+   it (overriding whatever color classes the individual cell renderers use),
+   so an unread ticket is unmistakable at a glance. */
+.hd-unread-row,
+.hd-unread-row * {
+  background-color: black !important;
+  color: white !important;
+}
+</style>
diff --git a/desk/src/components/ListViewBuilder.vue b/desk/src/components/ListViewBuilder.vue
index eb9d2ca..46220a4 100644
--- a/desk/src/components/ListViewBuilder.vue
+++ b/desk/src/components/ListViewBuilder.vue
@@ -73,6 +73,7 @@
       :rows="rows"
       v-slot="{ idx, column, item, row }"
       :group-by-actions="options.groupByActions"
+      :highlight-unread="options.highlightUnread"
       @scrollend="handleListScroll"
       class="list-rows"
     >
diff --git a/desk/src/components/ticket-agent/TicketActivityPanel.vue b/desk/src/components/ticket-agent/TicketActivityPanel.vue
index 396d3f4..5dddb0b 100644
--- a/desk/src/components/ticket-agent/TicketActivityPanel.vue
+++ b/desk/src/components/ticket-agent/TicketActivityPanel.vue
@@ -22,8 +22,8 @@
     ref="communicationAreaRef"
     :ticketId="String(ticket.doc?.name)"
     :to-emails="[ticket.doc?.raised_by]"
-    :cc-emails="[]"
-    :bcc-emails="[]"
+    :cc-emails="splitEmails(ticket.doc?.cc)"
+    :bcc-emails="splitEmails(ticket.doc?.bcc)"
     :key="ticket.doc?.name"
     @update="reloadTicketFeed(String(ticket.doc?.name))"
   />
@@ -50,6 +50,16 @@ import TicketTimeline from "./timeline/TicketTimeline.vue";
 
 const ticket = inject(TicketSymbol)!;
 
+// Ticket-level CC/BCC (set on the main ticket form) are used as the default
+// recipients on every reply, so agents don't have to retype them each time.
+function splitEmails(value?: string): string[] {
+  if (!value) return [];
+  return value
+    .split(",")
+    .map((email) => email.trim())
+    .filter(Boolean);
+}
+
 const communicationAreaRef = ref<InstanceType<typeof CommunicationArea> | null>(
   null
 );
diff --git a/desk/src/components/ticket-agent/TicketDetailsTab.vue b/desk/src/components/ticket-agent/TicketDetailsTab.vue
index 4878f5f..6e981a1 100644
--- a/desk/src/components/ticket-agent/TicketDetailsTab.vue
+++ b/desk/src/components/ticket-agent/TicketDetailsTab.vue
@@ -182,7 +182,14 @@ const { notifyTicketUpdate } = useNotifyTicketUpdate(ticket.value?.name);
 const dateFormat = window.date_format;
 const { getStatus, colorMap } = useTicketStatusStore();
 
-const CORE_FIELDS = ["priority", "ticket_type", "customer", "agent_group"];
+const CORE_FIELDS = [
+  "priority",
+  "ticket_type",
+  "customer",
+  "agent_group",
+  "cc",
+  "bcc",
+];
 
 const coreFields = computed(() => {
   const fieldsMeta = getFields();
diff --git a/desk/src/pages/ticket/MobileTicketAgent.vue b/desk/src/pages/ticket/MobileTicketAgent.vue
index 3a68d42..b67ce9d 100644
--- a/desk/src/pages/ticket/MobileTicketAgent.vue
+++ b/desk/src/pages/ticket/MobileTicketAgent.vue
@@ -166,8 +166,8 @@
             v-model="ticket.doc"
             :ticketId="ticket.doc?.name"
             :to-emails="[ticket.doc.raised_by]"
-            :cc-emails="[]"
-            :bcc-emails="[]"
+            :cc-emails="splitEmails(ticket.doc?.cc)"
+            :bcc-emails="splitEmails(ticket.doc?.bcc)"
             :key="ticket.doc?.name"
             @update="
               () => {
@@ -305,6 +305,16 @@ const communicationAreaRef = ref<InstanceType<typeof CommunicationArea> | null>(
   null
 );
 
+// Ticket-level CC/BCC (set on the main ticket form) are used as the default
+// recipients on every reply, so agents don't have to retype them each time.
+function splitEmails(value?: string): string[] {
+  if (!value) return [];
+  return value
+    .split(",")
+    .map((email) => email.trim())
+    .filter(Boolean);
+}
+
 const subjectInput = ref(null);
 const showPhoneModal = ref(false);
 const customActions = ref([]);
diff --git a/desk/src/pages/ticket/Tickets.vue b/desk/src/pages/ticket/Tickets.vue
index 1e1ce66..9442552 100644
--- a/desk/src/pages/ticket/Tickets.vue
+++ b/desk/src/pages/ticket/Tickets.vue
@@ -224,6 +224,10 @@ const options = computed(() => ({
   selectable: true,
   showSelectBanner: true,
   selectBannerActions,
+  // A ticket the current user hasn't opened yet gets a solid black row with
+  // white text in the list, instead of just semibold text, so it's obvious
+  // at a glance which tickets are new/updated - like an unread email.
+  highlightUnread: true,
   emptyState: {
     title: __("No tickets found"),
     icon: h(TicketIcon, {
diff --git a/desk/src/types.ts b/desk/src/types.ts
index 7f6e47e..d2cbfae 100644
--- a/desk/src/types.ts
+++ b/desk/src/types.ts
@@ -199,6 +199,8 @@ export interface Ticket {
   name: string;
   priority: string;
   raised_by: string;
+  cc: string;
+  bcc: string;
   resolution_by: string;
   response_by: string;
   first_responded_on: string;
diff --git a/desk/src/types/doctypes.ts b/desk/src/types/doctypes.ts
index 62596b7..8d00710 100644
--- a/desk/src/types/doctypes.ts
+++ b/desk/src/types/doctypes.ts
@@ -37,6 +37,10 @@ export interface HDTicket extends DocType {
   subject: string;
   /** Raised By (Email): Data */
   raised_by?: string;
+  /** CC: Small Text */
+  cc?: string;
+  /** BCC: Small Text */
+  bcc?: string;
   /** Status: Link (HD Ticket Status) */
   status?: string;
   /** Priority: Link (HD Ticket Priority) */
diff --git a/helpdesk/helpdesk/doctype/hd_ticket/hd_ticket.json b/helpdesk/helpdesk/doctype/hd_ticket/hd_ticket.json
index 16c8c13..85960b2 100644
--- a/helpdesk/helpdesk/doctype/hd_ticket/hd_ticket.json
+++ b/helpdesk/helpdesk/doctype/hd_ticket/hd_ticket.json
@@ -12,6 +12,8 @@
   "subject_section",
   "subject",
   "raised_by",
+  "cc",
+  "bcc",
   "status",
   "priority",
   "status_category",
@@ -98,6 +100,18 @@
    "oldfieldtype": "Data",
    "options": "Email"
   },
+  {
+   "fieldname": "cc",
+   "fieldtype": "Small Text",
+   "label": "CC",
+   "description": "Comma separated list of email addresses to CC on every reply for this ticket."
+  },
+  {
+   "fieldname": "bcc",
+   "fieldtype": "Small Text",
+   "label": "BCC",
+   "description": "Comma separated list of email addresses to BCC on every reply for this ticket."
+  },
   {
    "fieldname": "status",
    "fieldtype": "Link",
